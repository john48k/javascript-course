// the guideline

// = assignment operator
// == comparison operator (compares values if they are equal)
// === strict equality operator (this compares the values and the data types as well if they are all equal)
// != inequality operator
// !== strict inequality operator

const PI = 3.14;

if (PI == "3.14") {
  console.log("That is pie");
} else {
  console.log("That is not pie");
}
